import { Card, Typography } from '@mui/material'
import React from 'react'

const CheckItemCard = ({open,setOpen}) => {
  return (
    <Card open={open}>
        <Typography>
        </Typography>
    </Card>
  )
}

export default CheckItemCard
